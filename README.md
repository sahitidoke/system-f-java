Greetings

This is an implementation of System-F in Java. The Main.java file has all the "unit" tests. It runs through each one of the evaluation rules and typing rules and prints out the input term, expected and actual evaluated result in the terminal. 
After cloning the repo, running javac Main.java and java Main in terminal should be enough.

