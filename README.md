Greetings

This is an implementation of System-F in Java. The Main.java file has all the "unit" tests. First, it runs through each one of the evaluation rules and prints out the input term, expected and actual evaluated result in the terminal.
